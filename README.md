# AI Gateway

Simple AI Gateway for trying various ideas.

Pass requests through it, get automatic logging, cost tracking, and more.

## Stress testing

The gateway includes configurable dataset-scale log/tag benchmarks and an
end-to-end HTTP/RPS load harness with a deterministic mock provider. See the
[stress-test guide](apps/gateway-backend/tests/stress/README.md).
